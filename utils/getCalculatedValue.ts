export const getCalculatedValue = (value: string, allTokens: object) => {
  const getCalculatedValue = (value: string, tokens: any): string => {
    let math: string = '';
    let left: string = '';
    let right: string = '';
    let calculated: string = '';

    const traverse = (left: string, math: string, right: string) => {};
  };
};
